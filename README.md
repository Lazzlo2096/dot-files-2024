# First run :

```sh
cd ~ ; git clone https://github.com/Lazzlo2096/dot-files-2024.git ; cd ~/dot-files-2024 ; 
./install_or_update.sh
```

# Update:

```sh
cd ~/dot-files-2024 ; 
git pull
./install_or_update.sh
```